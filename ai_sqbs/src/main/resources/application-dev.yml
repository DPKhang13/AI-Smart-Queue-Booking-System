app:
  cookie:
    refresh-token-name: sq_refresh_token
    refresh-token-path: /api/auth
    http-only: true
    secure: false
    same-site: Strict

spring:
  docker:
    compose:
      enabled: false
  flyway:
    baseline-on-migrate: true
